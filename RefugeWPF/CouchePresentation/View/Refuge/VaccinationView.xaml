<UserControl x:Class="RefugeWPF.CouchePresentation.View.Refuge.VaccinationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RefugeWPF.CouchePresentation.View.Refuge"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30px" />
            <RowDefinition Height="50px" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Titre de page -->
        <TextBlock 
            Grid.Row="0"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            FontFamily="Bell MT"
            Text="{Binding Title}" 
        />


        <!--==== Barre de recherche =============================================-->
        <StackPanel 
            x:Name="SearchAnimalForm"
            Grid.Row="1"
            Orientation="Horizontal"
            Height="50" 
            Background="BlueViolet" 
        >
            <TextBlock 
                Text="Nom de l'animal" 
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="10,0"
                FontSize="14"
                FontWeight="Bold"
            />

            <TextBox 
                Width="200"
                Height="30"
    
            />

            <Button 
            Content="Recherche"
            Margin="10,0"
            FontWeight="SemiBold"
            FontSize="12"
            Height="32"
            Background="BlueViolet"
            Foreground="White"
            BorderThickness="2"
            VerticalAlignment="Center"
        />
        </StackPanel>

        <!--======= Main Content ============================================-->
        <Grid 
            Grid.Row="2"
        >

            <Grid.RowDefinitions>
                <RowDefinition Name="RowListVaccination" MaxHeight="500px"/>
                <RowDefinition Height="50px"/>
                <RowDefinition Name="RowFormVaccination" Height="300px"/>
            </Grid.RowDefinitions>

            <!--========== Liste des admissions =========================================================-->
            <StackPanel
                Grid.Row="0"
                Orientation="Vertical"
            >
                <!-- Titre de la liste -->
                <TextBlock 
                    Text="Liste des vaccinations"
                    HorizontalAlignment="Center"
                    Margin="0,5"
                    FontSize="16"
                    FontWeight="Bold"
                    FontFamily="Segoe UI"
                />

                <!--========== Liste des vaccinations =========================================================-->
                <DataGrid
                    Name="Vaccinations_DataGrid"
                    Margin="5"
                    Height="380"
                    HorizontalAlignment="Center"
                    IsReadOnly="True"
                    ItemsSource="{Binding Vaccinations}"
                    SelectedItem="{Binding VaccinationsDataGridSelectedItem}"
                    AutoGenerateColumns="False"
                >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date de création" Binding="{Binding DateCreated}" Width="120" />
                        <DataGridTextColumn Header="Date de vaccination" Binding="{Binding DateVaccination}" Width="150"/>
                        <DataGridCheckBoxColumn Header="Réalisé" Binding="{Binding Done}" Width="150"/>
                        <DataGridTextColumn Header="Nom du vaccin" Binding="{Binding Vaccine.Name}" Width="150"/>
                        <DataGridTextColumn Header="Nom de l'animal" Binding="{Binding Animal.Name}" Width="150"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Animal.Type}" Width="80"/>
                        <DataGridTextColumn Header="Genre" Binding="{Binding Animal.Gender}" Width="50"/>
                        <DataGridTextColumn Header="Couleurs" Binding="{Binding Animal.AnimalColors, Converter={StaticResource AnimalColorsToStringConverter}}" />
                        <DataGridTextColumn Header="Date de naissance" Binding="{Binding Animal.BirthDate}" Width="120" />
                        <DataGridTextColumn Header="Date de décès" Binding="{Binding Animal.DeathDate}" Width="120" />
                        <DataGridCheckBoxColumn Header="Stérilisé" Binding="{Binding Animal.IsSterilized}" Width="80" />
                        <DataGridTextColumn Header="Date de stérilisation" Binding="{Binding Animal.DateSterilization}" />
                        <DataGridTextColumn Header="Particularité" Binding="{Binding Animal.Particularity}"  Width="180"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Animal.Description}" Width="180"/>


                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <!--======== Menu de bouton ====================================================-->
            <StackPanel 
                Grid.Row="1"
                Background="BlueViolet" 
                Orientation="Horizontal" 
                HorizontalAlignment="Center"
            >

                <Button 
                    Content="Ajouter"
                    Margin="10,0"
                    FontWeight="SemiBold"
                    FontSize="12"
                    Height="32"
                    Background="BlueViolet"
                    Foreground="White"
                    BorderThickness="2"
                    VerticalAlignment="Center"
                    Click="EnableCreateAdoption"
                />

                


            </StackPanel>

            <!--========= Panneau en bas de page ===========================================-->

            <StackPanel
            Name="VaccinationFormSection"
            Grid.Row="2"
            Orientation="Vertical"
            Margin="10,5,5,5"
    
        >
                <StackPanel.Background>
                    <SolidColorBrush Color="Wheat" Opacity="0.7" />
                </StackPanel.Background>

                <TextBlock 
                    Text="{Binding TitleForm}"
                    Margin="0,10,0,5"
                    HorizontalAlignment="Center"
                    Padding="5"
                    FontSize="16"
                    FontWeight="Bold"
                    FontFamily="Segoe UI"
                    Foreground="BlueViolet"
                />

                <ScrollViewer 
                    VerticalScrollBarVisibility="Visible"
                    Margin="10,0,0,0"
                    Height="300"
                >
                    <StackPanel
                        Orientation="Vertical"    
                    >
                        <!--========= Formulaire Vaccination =================================================-->
                        <GroupBox
                            Name="FormVaccinationSection"
                            Header="Vaccination"
                            Margin="10"
                            FontSize="14"
                            FontFamily="Segoe UI"
                            FontWeight="Bold"
                        >
                            <StackPanel
                                Orientation="Vertical"
                            >
                                
                                <StackPanel
                                    Orientation="Horizontal"    
                                >
                                    <!--===== Formulaire Vaccination - Date de vaccination ======================================-->
                                    <StackPanel
                                        Orientation="Vertical"
                                        Margin="10,5"       
                                    >
                                        <TextBlock 
                                            Text="Date de vaccination"
                                            FontSize="14"
                                            FontFamily="Segoe UI"
                                            FontWeight="Bold"
                                            Margin="10,0"
                                            VerticalAlignment="Center"
                                        />
                                        <DatePicker
                                            Name="DateVaccination_DatePicker"
                                            Width="250"
                                            SelectedDate="{Binding DateVaccination, UpdateSourceTrigger=PropertyChanged}"
                                        />
                                    </StackPanel>

                                    <!--===== Formulaire Vaccination - Réalisé ======================================-->
                                    <StackPanel
                                        Orientation="Vertical"  
                                        Margin="10,5"
                                    >
                                        <TextBlock 
                                            Text="Réalisé"
                                            FontSize="14"
                                            FontFamily="Segoe UI"
                                            FontWeight="Bold"
                                            Margin="10,0"
                                            VerticalAlignment="Center"
                                        />
                                        <CheckBox
                                            Name="AdoptionDateEnd_DatePicker"
                                            Width="250"
                                            Margin="10,5"
                                            IsChecked="{Binding Done, UpdateSourceTrigger=PropertyChanged}"
                                        />
                                    </StackPanel>
                                </StackPanel>



                            </StackPanel>
                        </GroupBox>

                        <!-- ===== Formulaire vaccin ======================================= -->
                        <GroupBox
                            Name="FormVaccineSection"
                            Header="Vaccin"
                            Margin="10"
                            FontSize="14"
                            FontFamily="Segoe UI"
                            FontWeight="Bold"
                        >
                            <StackPanel
                                Orientation="Horizontal"
                            >
                                <TextBlock 
                                    Text="Nom du vaccin"
                                    FontSize="14"
                                    FontFamily="Segoe UI"
                                    FontWeight="Bold"
                                    Margin="10,0"
                                    VerticalAlignment="Center"
                                />
                                <ComboBox 
                                    Name="VaccinationVaccine_ComboBox"
                                    Width="250"
                                    DisplayMemberPath="Name"
                                    ItemsSource="{Binding Vaccines, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedItem="{Binding SelectedVaccine, UpdateSourceTrigger=PropertyChanged}"
                                />

                                <Button
                                    Margin="10,5"
                                    Background="BlueViolet"
                                    Foreground="White"
                                    FontSize="12"
                                    Content="Valider"
                                    Click="SearchVaccineButton_Click"
                                ></Button>

                                <!-- Affichage du résultat de la recherche de vaccin -->
                                <TextBlock 
                                    Name="ContactFullnameDisplay"
                                    FontFamily="Segoe UI"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    VerticalAlignment="Center"
                                >
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="Nom du vaccin :  {0}">
                                            <Binding Path="VaccineFound.Name" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>

                            

                                
                        </GroupBox>

                        <!--========= Formulaire animal =================================================-->
                        <GroupBox
                            Name="FormAnimalSection"
                            Header="Animal"
                            Margin="10"   
                            FontSize="14"
                            FontFamily="Segoe UI"
                            FontWeight="Bold"
                        >
                            <StackPanel
                                Orientation="Vertical"    
                            >
                                <!--========= Formulaire recherche animal existant =================================================-->
                                <StackPanel
                                    Name="FormSearchAnimalSection"
                                    Margin="0,5"
                                >

                                    <StackPanel
                                        Orientation="Horizontal"
                                
                                    >
                                        <TextBlock 
                                            Text="Nom de l'animal"
                                            FontSize="14"
                                            FontFamily="Segoe UI"
                                            FontWeight="Bold"
                                            Margin="10,0"
                                            VerticalAlignment="Center"
                                        />
                                        <TextBox 
                                            Name="AnimalSearchByName_Textbox"
                                            Width="250"
                                            Text="{Binding FormAnimalName}"
                                        />

                                        <Button
                                            Margin="10,5"
                                            Background="BlueViolet"
                                            Foreground="White"
                                            FontSize="12"
                                            Content="Valider"
                                            Click="SearchAnimalButton_Click"
                                        ></Button>

                                    </StackPanel>
                                    <!-- Affichage du résultat de la recherche de la personne de contact -->
                                    <StackPanel>
                                        <DataGrid
                                            Name="AnimalsFound_DataGrid"
                                            ItemsSource="{Binding AnimalsFound, UpdateSourceTrigger=PropertyChanged}"
                                            SelectedItem="{Binding AnimalFound, UpdateSourceTrigger=PropertyChanged}"
                                            IsReadOnly="True"
                                            AutoGenerateColumns="False"
                                        >
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="150"/>
                                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                                                <DataGridTextColumn Header="Genre" Binding="{Binding Gender}" Width="50"/>
                                                <DataGridTextColumn Header="Couleurs" Binding="{Binding AnimalColors, Converter={StaticResource AnimalColorsToStringConverter}}" />
                                                <DataGridTextColumn Header="Date de naissance" Binding="{Binding BirthDate}" Width="120" />
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>

                                </StackPanel>


                            </StackPanel>



                        </GroupBox>


                    </StackPanel>


                </ScrollViewer>
                <!-- Formulaire - Bouton d'action -->
                <StackPanel
                HorizontalAlignment="Center"    
            >
                    <Button
                    Margin="0,5" 
                    Background="GreenYellow"
                    Click="SubmitButton_Click"
                >
                        Confirmer
                    </Button>
                </StackPanel>


            </StackPanel>


        </Grid>



    </Grid>
</UserControl>
